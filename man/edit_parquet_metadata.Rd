% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-metadata.R
\name{edit_parquet_metadata}
\alias{edit_parquet_metadata}
\title{Edit the key-value metadata of a Parquet file}
\usage{
edit_parquet_metadata(file, metadata)
}
\arguments{
\item{file}{Path to a Parquet file.}

\item{metadata}{Key-value pairs to add, update or remove. Accepted forms:
\itemize{
\item A \strong{named character vector} – each name is a key and each element is
its new value. Set a value to \code{NA} to \strong{remove} that key.
\item A \strong{data frame with two columns} – the first column contains keys and
the second contains values. An \code{NA} value removes that key.
\item \code{NULL} – removes \strong{all} key-value metadata from the file.
}

Keys that are not mentioned in \code{metadata} are left unchanged.}
}
\value{
\code{NULL}, invisibly.  Called for its side effect.
}
\description{
Modify the file-level key-value metadata of a Parquet file in place,
without copying any row-group data. Individual entries can be added,
updated or removed.
}
\section{Warning}{

This function is \strong{not} atomic! If it is interrupted it may leave the
file in a corrupt state. To work around this create a copy of the
original file, edit the copy, and then rename the copy to the original.
}

\examples{
tmp <- tempfile(fileext = ".parquet")
write_parquet(data.frame(x = 1:5), tmp)

# Add a new entry
edit_parquet_metadata(tmp, c(created_by_user = "Alice"))
read_parquet_metadata(tmp)$file_meta_data$key_value_metadata

# Update an existing entry
edit_parquet_metadata(tmp, c(created_by_user = "Bob"))
read_parquet_metadata(tmp)$file_meta_data$key_value_metadata

# Remove an entry
edit_parquet_metadata(tmp, c(created_by_user = NA_character_))
read_parquet_metadata(tmp)$file_meta_data$key_value_metadata
}
\seealso{
\code{\link[=read_parquet_metadata]{read_parquet_metadata()}} to inspect metadata.
\code{\link[=write_parquet]{write_parquet()}} for the \code{metadata} argument when writing.
\code{\link[=append_parquet]{append_parquet()}} to append rows without copying.
}
